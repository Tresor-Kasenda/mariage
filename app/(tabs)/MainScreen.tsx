import React from 'react';
import { Text, View } from 'react-native';

const MainScreen = () => {
  return (
    <View className='flex-1 justify-center items-center bg-white'>
      <Text className='font-semibold'>Main Screen</Text>
    </View>
  );
};

export default MainScreen;
